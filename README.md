# Blog created by React + TypeScript + Vite template

Command for create project

```shell
npm create vite@latest . -- --template react-ts 
```

Command for install

```shell
npm install
```

Commad for run

```shell
 npm run dev
```
